# Copy this file to .env and set real values before running the server
OPENAI_API_KEY=your_openai_api_key_here
# Optional simple API token for admin actions (basic protection)
ADMIN_TOKEN=change_me_to_a_secure_token
PORT=4000
